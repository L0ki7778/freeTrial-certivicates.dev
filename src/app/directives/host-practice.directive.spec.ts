import { HostPracticeDirective } from './host-practice.directive';

describe('HostPracticeDirective', () => {
  it('should create an instance', () => {
    const directive = new HostPracticeDirective();
    expect(directive).toBeTruthy();
  });
});
